// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: © 2026 James Cropcho <numerate_penniless652@dralias.com>
'use strict';

const globals = require('globals');
const js = require('@eslint/js');
const tseslint = require('typescript-eslint');
const { defineConfig } = require('eslint/config');

const commonRules = {
  'brace-style': [2, '1tbs', { 'allowSingleLine': true }],
  'curly': ['error', 'all'],
  'eqeqeq': ['error', 'always'],
  'guard-for-in': 'error',
  'indent': [2, 2],
  'linebreak-style': ['error', 'unix'],
  'no-restricted-syntax': ['error',
    {
      selector: 'CallExpression[callee.name="Function"][arguments.0.value="return this"]',
      message: 'Capture the existing global object or use globalThis instead of Function(\'return this\').',
    },
    {
      selector: 'BinaryExpression[operator="!=="][left.type="CallExpression"][left.callee.property.name="indexOf"][right.value=-1]',
      message: 'Use includes() instead of indexOf() !== -1 for presence checks.',
    },
    {
      selector: 'BinaryExpression[operator="==="][left.type="CallExpression"][left.callee.property.name="indexOf"][right.value=-1]',
      message: 'Use !includes() instead of indexOf() === -1 for absence checks.',
    },
    {
      selector: 'BinaryExpression[operator=">"][left.type="CallExpression"][left.callee.property.name="indexOf"][right.value=-1]',
      message: 'Use includes() instead of indexOf() > -1 for presence checks.',
    },
    {
      selector: 'BinaryExpression[operator=">="][left.type="CallExpression"][left.callee.property.name="indexOf"][right.value=0]',
      message: 'Use includes() instead of indexOf() >= 0 for presence checks.',
    },
    {
      selector: 'BinaryExpression[operator="<"][left.type="CallExpression"][left.callee.property.name="indexOf"][right.value=0]',
      message: 'Use !includes() instead of indexOf() < 0 for absence checks.',
    },
  ],
  'quotes': ['error', 'single'],
  'semi': ['error', 'always'],
};

const nodeModernizationRules = {
  'no-throw-literal': 'error',
  'no-var': 'error',
  'object-shorthand': ['error', 'always'],
  'prefer-const': 'error',
  'prefer-object-has-own': 'error',
  'prefer-template': 'error',
};

module.exports = [
  { ignores: ['.claude/'] },
  js.configs.recommended,
  {
    files: ['**/*.js', 'bin/variety'],
    languageOptions: {
      sourceType: 'script',
      globals: {
        ...globals.node,
        ...globals.mocha,
        ...globals.mongo,
        __quiet: 'readonly',
        slaveOk: 'readonly',
        collection: 'readonly',
        DBQuery: 'readonly',
        BinData: 'readonly',
        tojson: 'readonly',
      },
    },
    rules: commonRules,
  },
  {
    files: ['test/**/*.js'],
    languageOptions: {
      sourceType: 'module',
    },
  },
  {
    files: ['.eslint.config.js', 'bin/variety', 'build.js', 'cli/**/*.js', 'mongo-shell/launcher.js', 'test/**/*.js'],
    ignores: ['test/fixtures/**/*.js'],
    rules: nodeModernizationRules,
  },
  {
    files: ['variety.js', 'core/analyzer.js', 'mongo-shell/adapter.js'],
    // Rules safe for the ES6+ compatibility floor declared at the top of variety.js.
    // prefer-object-has-own is excluded because Object.hasOwn() (ES2022) is absent
    // from the legacy mongo shell; see the compatibility comment in variety.js.
    // variety.js is generated by build.js from the core/analyzer.js and
    // mongo-shell/adapter.js source files above; we lint both the sources
    // and the committed build output so either entry point catches regressions.
    rules: {
      'no-throw-literal': 'error',
      'no-var': 'error',
      'object-shorthand': ['error', 'always'],
      'prefer-const': 'error',
      'prefer-template': 'error',
    },
  },
  ...defineConfig({
    files: ['test/**/*.js'],
    ignores: ['test/fixtures/**/*.js'],
    // Keep shell-executed fixtures out of the typed Node-side ruleset.
    extends: [tseslint.configs.recommendedTypeChecked],
    languageOptions: {
      parserOptions: {
        project: './.tsconfig.checkjs.json',
        tsconfigRootDir: __dirname,
      },
    },
  }),
];
